version: '2'
services:
  opencellarbook-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/OpenCellarBook/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=OpenCellarBook
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
